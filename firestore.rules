rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny all
    match /{document=**} {
      allow read, write: if false;
    }

    // CORRECTED USER RULES (deduplicated)
    match /users/{userId} {
      allow read: if request.auth != null && request.auth.uid == userId;
      // Owner can update own profile EXCEPT server-only fields
      allow update: if request.auth != null && request.auth.uid == userId &&
        !(('kycStatus' in request.resource.data.diff(resource.data).changedKeys()) ||
          ('trustScore' in request.resource.data.diff(resource.data).changedKeys()) ||
          ('farmCertification' in request.resource.data.diff(resource.data).changedKeys()) ||
          ('verifiedTransfersCount' in request.resource.data.diff(resource.data).changedKeys()) ||
          ('profileBadges' in request.resource.data.diff(resource.data).changedKeys()));
      // Creation allowed for self
      allow create: if request.auth != null && request.auth.uid == userId;
      // Note: Region-level privacy enforcement is handled in app logic; sensitive
      // location precision should not be written when regionPrivacy == true.
    }

    // CORRECTED FOWL RULES + RECORDS NESTING
    match /fowls/{fowlId} {
      allow read, write: if request.auth != null && request.auth.uid == resource.data.ownerId;

      match /records/{recordId} {
        allow read, write: if request.auth != null &&
          request.auth.uid == get(/databases/$(database)/documents/fowls/$(fowlId)).data.ownerId &&
          request.resource.data.fowlId == fowlId;
      }
    }

    // Marketplace Listings (unchanged)
    match /marketplace_listings/{listingId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null && request.resource.data.sellerId == request.auth.uid;
      allow update, delete: if request.auth != null && resource.data.sellerId == request.auth.uid;
    }

    // UNIFIED TRANSFER RULES
    match /transfers/{transferId} {
      // Read access: only transfer participants
      allow read: if request.auth != null &&
        (request.auth.uid == resource.data.fromUserId || request.auth.uid == resource.data.toUserId);

      // Create: only sender can initiate with PENDING status
      allow create: if request.auth != null &&
        request.auth.uid == request.resource.data.fromUserId &&
        request.resource.data.status == 'PENDING' &&
        request.resource.data.keys().hasAll(['fowlId', 'toUserId', 'requestTimestamp']);

      // Update: controlled state transitions only
      allow update: if request.auth != null &&
        (
          // Sender can cancel pending transfers
          (request.auth.uid == resource.data.fromUserId && resource.data.status == 'PENDING' && request.resource.data.status == 'CANCELED') ||
          // Receiver can reject pending transfers
          (request.auth.uid == resource.data.toUserId && resource.data.status == 'PENDING' && request.resource.data.status == 'REJECTED')
        ) &&
        // Prevent modification of immutable fields
        request.resource.data.fromUserId == resource.data.fromUserId &&
        request.resource.data.toUserId == resource.data.toUserId &&
        request.resource.data.fowlId == resource.data.fowlId &&
        request.resource.data.requestTimestamp == resource.data.requestTimestamp;
      // Note: COMPLETED is set by Cloud Functions only (approveTransfer)
    }

    // Conversations and Messages (unchanged)
    match /conversations/{conversationId} {
      allow read, update: if request.auth != null && request.auth.uid in resource.data.participantIds;
      allow create: if request.auth != null && request.auth.uid in request.resource.data.participantIds;

      match /messages/{messageId} {
        allow read: if request.auth != null && request.auth.uid in get(/databases/$(database)/documents/conversations/$(conversationId)).data.participantIds;
        allow create: if request.auth != null && request.resource.data.senderId == request.auth.uid && request.auth.uid in get(/databases/$(database)/documents/conversations/$(conversationId)).data.participantIds;
      }
    }

    // Analytics daily snapshots per user (keep as-is)
    match /analytics/{userId}/daily/{date} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // User analytics event logs (written by Cloud Functions only)
    match /user_analytics/{userId}/events/{eventId} {
      allow read: if request.auth != null && request.auth.uid == userId;
      allow write: if false; // Cloud Functions only
    }

    // ADMIN COLLECTIONS (locked to Functions/admin infra only)
    // If you later need console access, consider role-based checks.
    match /fraud_analysis/{document=**} {
      allow read, write: if false;
    }
    match /admin_alerts/{document=**} {
      allow read, write: if false;
    }
    match /admin_actions/{document=**} {
      allow read, write: if false;
    }
    match /transfer_audit_logs/{logId} {
      allow read: if false; // Admin-only via backend tools
      allow write: if false; // Cloud Functions (Admin SDK) only
    }
  }
}
